1. Read through the user's issue again: image generation when uploading an image sometimes drops the generated image and it is not stored. It also drops state on navigation or backgrounding.
2. The UI doesn't resume stream loading nicely if navigation happens.
3. In `ChatLayout.tsx`, `ChatArea` is currently unmounted when `isImaginePage` is true: `{!isImaginePage && <ChatArea />}`. This violates the rule mentioned in memory: "To prevent active chat streams from aborting during navigation, the `<ChatArea>` component is kept mounted but hidden (e.g., using Tailwind's `hidden` class) inside `ChatLayout.tsx` when switching to non-chat routes like `/imagine`."
4. In `ChatArea.tsx`, the `streamStore` is imported but it's not being used for `isGeneratingImage` or `generatedImage` or the stream text directly. Wait, the rule says "chat streaming state is managed globally via `src/lib/streamStore.ts`... and consumed with `useSyncExternalStore`". But `ChatArea` uses `useState` for all of this right now.
