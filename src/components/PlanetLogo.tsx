import React from 'react';

export const PlanetLogo = ({ className = "" }: { className?: string }) => (
  <div className={`font-bold text-xl tracking-tight text-white flex items-center justify-center select-none ${className}`}>
    Chris
  </div>
);
